import { CarPricePipePipe } from './car-price-pipe.pipe';

describe('CarPricePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CarPricePipePipe();
    expect(pipe).toBeTruthy();
  });
});
