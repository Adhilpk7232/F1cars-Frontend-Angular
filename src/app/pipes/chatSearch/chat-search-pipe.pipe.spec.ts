import { ChatSearchPipePipe } from './chat-search-pipe.pipe';

describe('ChatSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
