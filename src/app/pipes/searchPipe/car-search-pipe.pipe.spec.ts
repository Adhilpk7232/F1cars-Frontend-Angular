import { CarSearchPipePipe } from './car-search-pipe.pipe';

describe('CarSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CarSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
